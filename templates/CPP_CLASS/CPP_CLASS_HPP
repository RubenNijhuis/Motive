#ifndef {{loud name}}_HPP
# define {{loud name}}_HPP

class {{name}}
{
	public:
		{{name}}(void);
		{{name}}(const {{name}}& other);
		~{{name}}(void);

		{{name}} &operator=(const {{name}}& other);
};

#endif